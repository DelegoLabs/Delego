echo "🔍 Running pre-commit checks..."

# Run lint
echo "▸ Running linter..."
node_modules/.pnpm/node_modules/.bin/pnpm run lint
if [ $? -ne 0 ]; then
  echo "❌ Linting failed."
  exit 1
fi

echo "✅ Pre-commit checks passed!"
