{
  "name": "delego",
  "version": "0.0.1",
  "private": true,
  "description": "Frontend web application for the Delego AI commerce platform",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm --filter @delegolabs/web dev",
    "dev:web": "pnpm --filter @delegolabs/web dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "pnpm -r exec rm -rf dist .next node_modules && rm -rf .turbo",
    "prepare": "husky",
    "seed:demo": "pnpm --filter @delegolabs/web seed:demo"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2",
    "@types/node": "^25.9.3",
    "husky": "^9.1.7",
    "turbo": "^2.3.0"
  },
  "pnpm": {
    "overrides": {
      "axios": ">=1.16.0"
    }
  }
}
